{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "public/**"],
      "env": []
    },
    "ultracite:check": {
      "outputs": [],
      "inputs": ["biome.jsonc", "src/**"]
    },
    "type:check": {
      "outputs": ["*.tsbuildinfo"]
    }
  }
}
